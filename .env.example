# Wellio Health Environment Variables
# 
# NOTE: This file contains ONLY publishable/public keys.
# All secrets (API keys, service role keys) are stored in Lovable Cloud Secrets,
# NOT in environment files. This is secure by design.
#
# Copy this file to .env for local development:
#   cp .env.example .env

# Supabase Configuration (Publishable Keys Only)
# These are safe to expose - they are anon/public keys designed for client-side use
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# Note: The following secrets are stored in Lovable Cloud Secrets, NOT here:
# - SUPABASE_SERVICE_ROLE_KEY (for edge functions)
# - MEDICAL_ENCRYPTION_KEY (for quantum-resistant encryption)
# - OPENAI_API_KEY (for AI voice features)
# - STRIPE_SECRET_KEY (for payments)
# - Payment provider credentials
