// Re-export for backward compatibility
export { PageLoadingSkeleton } from '../LoadingSkeleton';
// Alias for backwards compatibility
export { PageLoadingSkeleton as LoadingSkeleton } from '../LoadingSkeleton';
